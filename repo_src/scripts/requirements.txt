# Core dependencies
asyncio>=3.4.3
aider-chat>=0.16.0

# Optional but recommended
python-dotenv>=1.0.0

# Add new requirements for multi-agent aider workflow
PyYAML>=6.0
google-generativeai>=0.3.0
anthropic>=0.8.0
tqdm>=4.65.0
setuptools
# Note: Don't specify openai separately as aider-chat depends on a specific version
pip==25.0.1
